# Leave this unset in development so Vite proxies /api
# to the backend. That lets the UI use 5175 or the next free port.
# For a production build, set the full API URL:
# VITE_API_URL=http://localhost:5000/api/v1
